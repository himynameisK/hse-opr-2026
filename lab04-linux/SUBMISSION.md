# Занятие 4 — сдача
ФИО:
Группа:

## Задание 1. Пять сломанных скриптов

## Проверка

Где вы работали (виртуальная машина / WSL / контейнер) и какой дистрибутив:


Вывод `sudo /opt/opr-lab04/check.py`:

```text

```

## Пять поломок

Для каждого скрипта: чем вы её увидели (команда) и чем починили (команда).

`s1` — увидел:

`s1` — починил:


`s2` — увидел:

`s2` — починил:


`s3` — что показал `findmnt -T "$(readlink -f s3/script.sh)"`:

```text

```

`s3` — содержимое вашего `run.sh`:

```bash

```

`s4` — увидел:

`s4` — починил:


`s5` — увидел:

`s5` — починил:


## Вопросы

Почему поломку в `s5` не видно из-под root, хотя от `opr` она видна сразу?


Чем `./script.sh` отличается от `bash script.sh`: кто в каждом случае читает
шебанг и кто проверяет бит `x`?


Что именно случилось с `\r` в `s2` — какое имя ядро искало и почему?


Зачем на сервере монтируют `/tmp` с `noexec`, если обойти это может любой,
кто умеет писать `bash файл`?


Почему бит `x` на каталоге называют правом на поиск, а не правом на запуск?


## Задание 2. Общий каталог для команды

Команды, которыми настроили каталог:

```bash

```

`ls -ld /srv/shop/shared`:

```text

```

`ls -l /srv/shop/shared` после того, как alice и bob создали по файлу
(нужны владелец и группа):

```text

```

Что ответила система, когда alice попробовала удалить файл bob:

```text

```

## Вопросы

Почему право `w` на каталоге разрешает удалить файл, на который у вас нет
никаких прав, и какой бит это чинит?


Что означает `s` в `drwxrws--T`, что означает `T` и почему она заглавная?


Зачем каталогу SGID, если `alice` и `bob` и так состоят в группе `shop`?


Почему владельцем каталога оставили `root`? Что сможет `alice` с чужими файлами,
если каталог отдать ей, даже со sticky-битом?


`alice` создала файл, `bob` открыл его на запись и получил отказ, хотя оба
в одной группе и каталог настроен верно. Что произошло?
