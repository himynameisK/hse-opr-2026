# Занятие 3 — сдача

ФИО:

Группа:

## Часть 1

Вывод `./check.py`:

```text

```

## Часть 2. Свой репозиторий

Ссылка на репозиторий:

Заголовки доставки с webhook.site (`X-GitHub-Event`, `X-GitHub-Delivery`,
`X-Hub-Signature-256`):

```text

```

Вывод вашего `verify.py` на этой доставке и код возврата:

```text

```

Что показал `git commit`, когда вы попробовали закоммитить строку с токеном:

```text

```

## Вопросы

Почему `pre-commit` не защищает команду от утечки секрета, хотя у вас он работает?


Что произойдёт с вашим хуком у коллеги, который склонировал репозиторий?


Зачем вебхуку подпись, если адрес знаете только вы?
