# Занятие 3 — сдача

ФИО:

Группа:

Ссылка на ваш репозиторий на GitHub:


## Задание 1. Хуки

Вывод `./check.py --repo ...` — все семь строк:

```text

```

Что показал `git commit`, когда вы попробовали закоммитить строку с токеном:

```text

```

## Задание 2. Telegram

Текст сообщения, пришедшего в Telegram (токен затрите):

```text

```

Вывод шага из вашего `telegram.yml` — тело ответа Telegram и код возврата:

```text

```

## Задания 3 и 4. gitleaks

Ссылка на закрытый Pull Request, где проверка gitleaks стала красной:


Строка из лога шага gitleaks — файл, номер строки и правило:

```text

```

## Вопросы

Почему `pre-commit` не защищает команду от утечки секрета, хотя у вас он работает?


Что произойдёт с вашим хуком у коллеги, который склонировал репозиторий?


Почему для уведомления не понадобился свой сервер — кто кому звонит?


Что покажет проверка gitleaks, если удалить файл с ключом отдельным коммитом
и запушить удаление?
